import 'package:flutter/material.dart';

// Global navigator key — used to show SnackBars from anywhere (providers, services, etc.)
final GlobalKey<NavigatorState> navigatorKey = GlobalKey<NavigatorState>();